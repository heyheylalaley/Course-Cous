# Анализ мертвого кода

## Дата анализа
2025-01-27

## Найденные проблемы

### 1. Неиспользуемые компоненты

#### EmailConfirmationModal.tsx
- **Статус**: ❌ Не используется
- **Расположение**: `components/EmailConfirmationModal.tsx`
- **Описание**: Компонент для подтверждения email, но нигде не импортируется и не используется в приложении
- **Рекомендация**: Удалить файл, если функциональность не планируется использовать

#### NameModal.tsx
- **Статус**: ❌ Не используется
- **Расположение**: `components/NameModal.tsx`
- **Описание**: Модальное окно для ввода имени, но нигде не импортируется (в Dashboard.tsx есть только комментарий о нем)
- **Рекомендация**: Удалить файл, если функциональность не планируется использовать (возможно, заменен на FirstLoginProfileModal)

#### OnboardingModal.tsx
- **Статус**: ❌ Не используется
- **Расположение**: `components/OnboardingModal.tsx`
- **Описание**: Модальное окно для онбординга новых пользователей, но нигде не импортируется (возможно, заменен на FirstLoginProfileModal)
- **Рекомендация**: Удалить файл, если функциональность не планируется использовать

### 2. Неиспользуемые импорты

#### App.tsx
- **Импорт**: `Menu` из `lucide-react` (строка 15)
- **Статус**: ❌ Не используется
- **Описание**: Импортируется, но в компоненте не используется (используется только `X` для закрытия sidebar)
- **Рекомендация**: Удалить `Menu` из импорта

### 3. Неиспользуемые экспорты из хуков

#### hooks/useUserTour.ts
- **Функции**: `resetTour`, `nextStep`, `prevStep`
- **Статус**: ❌ Не используются
- **Описание**: Эти функции экспортируются из хука, но нигде не используются. Компонент UserTour использует `onStepChange` напрямую через пропсы, а не эти функции
- **Рекомендация**: Удалить неиспользуемые экспорты, если они не нужны для будущего использования

#### hooks/useDebounce.ts
- **Экспорт**: `export default useDebounce` (строка 25)
- **Статус**: ❌ Не используется
- **Описание**: Функция экспортируется как default export, но используется только именованный export (`import { useDebounce }`)
- **Рекомендация**: Удалить default export, если не планируется использование default импорта

### 4. Используемый код (не удалять)

#### constants.ts - AVAILABLE_COURSES
- **Статус**: ✅ Используется
- **Использование**: В `services/db.ts` (строки 848, 945, 1244)
- **Рекомендация**: Оставить

#### utils/courseUtils.ts - sortCoursesWithRegistrations
- **Статус**: ✅ Используется
- **Использование**: В `contexts/CoursesContext.tsx` (строка 49)
- **Рекомендация**: Оставить

#### hooks/useCourses.ts
- **Статус**: ✅ Используется
- **Использование**: В `components/Dashboard.tsx`, `components/AdminStudentList.tsx`, `components/AdminAllUsers.tsx`
- **Рекомендация**: Оставить

## Резюме

### Обнаружено:
- 3 неиспользуемых компонента (EmailConfirmationModal, NameModal, OnboardingModal)
- 1 неиспользуемый импорт (Menu в App.tsx)
- 3 неиспользуемых экспорта из хука (resetTour, nextStep, prevStep из useUserTour)
- 1 неиспользуемый default export (useDebounce)

### Общий размер мертвого кода:
- ~500-600 строк кода (3 компонента + неиспользуемые функции)

### Рекомендации:
1. Удалить неиспользуемые компоненты, если они точно не нужны
2. Удалить неиспользуемые импорты и экспорты
3. Перед удалением проверить, не используются ли они в других ветках или не планируются ли к использованию
