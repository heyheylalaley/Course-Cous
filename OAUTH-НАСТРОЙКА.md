# Настройка OAuth: Показ реального домена вместо Supabase

## Текущая ситуация

При авторизации через Google OAuth пользователи видят сообщение **"to continue to ugezbyszafkijwqifqlg.supabase.co"** вместо вашего реального домена сайта.

## Почему это происходит?

Google OAuth показывает домен, на который будет редирект после авторизации. Поскольку OAuth callback обрабатывается Supabase, Google видит домен Supabase. Это стандартное поведение для всех приложений, использующих Supabase на бесплатном плане.

## ✅ БЕСПЛАТНОЕ РЕШЕНИЕ: Cloudflare Worker

**Хорошие новости!** Можно использовать Cloudflare Worker для проксирования OAuth через ваш кастомный домен **бесплатно**!

### Как это работает

1. Cloudflare Worker проксирует OAuth запросы через ваш кастомный домен
2. Google видит ваш кастомный домен вместо Supabase
3. Worker перенаправляет запросы на Supabase прозрачно
4. Пользователи видят ваш домен в интерфейсе Google OAuth

### Быстрая настройка

1. **Создайте Cloudflare Worker:**
   - Перейдите в [Cloudflare Dashboard](https://dash.cloudflare.com/) → Workers & Pages
   - Создайте новый Worker
   - Скопируйте код из `cloudflare-worker-auth-proxy.js`
   - Установите переменные окружения:
     - `SUPABASE_URL`: ваш Supabase URL
     - `SITE_URL`: URL вашего сайта (например, `https://ccplearn.pages.dev`)

2. **Настройте кастомный домен:**
   - В Worker, добавьте Custom Domain (например, `auth.ccplearn.pages.dev`)
   - Или настройте маршрут через Workers Routes

3. **Обновите Google OAuth:**
   - В Google Cloud Console, добавьте redirect URI: `https://auth.yourdomain.com/auth/v1/callback`

4. **Обновите Supabase:**
   - Authentication → URL Configuration
   - Добавьте в Redirect URLs: `https://auth.yourdomain.com/**`

**Подробная инструкция:** См. `CLOUDFLARE-WORKER-OAUTH-SETUP.md`

## Что уже настроено правильно ✅

Ваш код уже правильно настроен:
- `redirectTo` указывает на ваш реальный домен
- После авторизации пользователи редиректятся на ваш сайт (не на Supabase)
- Сессия сохраняется корректно

## Что нужно проверить в Supabase Dashboard

1. **Перейдите в Supabase Dashboard:**
   - Authentication → URL Configuration

2. **Установите правильные URL:**
   - **Site URL**: Ваш реальный домен (например, `https://ccplearn.pages.dev/`)
   - **Redirect URLs**: Добавьте ваш домен с wildcard:
     - `https://ccplearn.pages.dev/**`
     - `https://auth.ccplearn.pages.dev/**` (если используете Worker)
     - `https://yourusername.github.io/yourrepository/**` (если используете GitHub Pages)
     - `http://localhost:5173/**` (для разработки)

3. **Проверьте Google OAuth настройки:**
   - Authentication → Providers → Google
   - Убедитесь, что Client ID и Client Secret правильно настроены

## Варианты решения

### Вариант 1: Cloudflare Worker (Рекомендуется, БЕСПЛАТНО) ✅

**Что дает:**
- Google будет показывать ваш кастомный домен вместо `supabase.co`
- Полностью бесплатно
- Работает на бесплатном плане Supabase
- Не требует изменений в коде приложения

**Требования:**
- Cloudflare аккаунт (бесплатный)
- Кастомный домен или `*.pages.dev` от Cloudflare

**Как настроить:**
См. подробную инструкцию в `CLOUDFLARE-WORKER-OAUTH-SETUP.md`

### Вариант 2: Оставить как есть (Бесплатно, но показывает Supabase)

**Что происходит:**
- Google показывает домен Supabase в интерфейсе выбора аккаунта
- После авторизации пользователь редиректится на ваш реальный сайт
- Все работает корректно

**Плюсы:**
- Бесплатно
- Работает надежно
- Стандартное поведение для Supabase

**Минусы:**
- Google показывает домен Supabase (но это не критично)

### Вариант 3: Кастомный домен для Supabase Auth (Платно) 💰

**Требования:**
- Платный план Supabase (Pro или выше) - ~$25/месяц
- Custom Domain Add-on - ~$10/месяц
- Итого: ~$35/месяц

**Что дает:**
- Google будет показывать ваш кастомный домен вместо `supabase.co`
- Профессиональный вид
- Полный контроль над доменом

**Как настроить:**
1. Supabase Dashboard → Settings → Custom Domains
2. Добавьте поддомен (например, `auth.yourdomain.com`)
3. Настройте DNS записи
4. После настройки Google будет показывать ваш домен

## Рекомендация

**Для бесплатного плана:** Используйте **Вариант 1 (Cloudflare Worker)**. Это полностью бесплатное решение, которое позволяет Google показывать ваш домен вместо Supabase.

**Если не хотите настраивать Worker:** Используйте Вариант 2. Хотя Google показывает домен Supabase, это не влияет на функциональность. Пользователи все равно редиректятся на ваш реальный сайт после авторизации.

**Для профессионального решения:** Если важно, чтобы Google показывал ваш домен, рассмотрите Вариант 3 (кастомный домен) или Вариант 1 (Cloudflare Worker).

## Дополнительная информация

- Подробная инструкция по Cloudflare Worker: `CLOUDFLARE-WORKER-OAUTH-SETUP.md`
- Подробная документация на английском: `OAUTH-CUSTOM-DOMAIN-SETUP.md`

## Проверка работы

После настройки проверьте:

1. ✅ Авторизация через Google работает
2. ✅ После авторизации пользователь редиректится на ваш реальный домен
3. ✅ Сессия сохраняется правильно
4. ✅ Пользователь может использовать приложение
5. ✅ (Если используете Worker) Google показывает ваш домен в интерфейсе выбора аккаунта

Если все работает, значит настройка правильная! 🎉
