# 📋 Инструкция по применению оптимизаций

## ✅ Что было сделано

Применены все 6 оптимизаций из списка:

1. ✅ `removeRegistration` - оптимизирован (N запросов → 1 запрос)
2. ✅ `updateRegistrationPriority` - оптимизирован (N запросов → 1 запрос)
3. ✅ `getAdminStudentDetails` - оптимизирован (2 запроса → 1 запрос с JOIN)
4. ✅ `getAllUsersWithDetails` - оптимизирован (3 запроса → 1 запрос с JOIN)
5. ✅ `getCourseQueues` - оптимизирован (2 запроса → 1 запрос)
6. ✅ `getCalendarEvents` - оптимизирован (2 запроса → 1 запрос с JOIN)

## 🚀 Шаги для применения

### Шаг 1: Применить SQL функции

1. Откройте **Supabase Dashboard** → **SQL Editor**
2. Скопируйте содержимое файла **`optimization-functions.sql`**
3. Вставьте в SQL Editor и выполните
4. Убедитесь, что все функции созданы успешно (в конце файла есть проверочный запрос)

### Шаг 2: Проверить обновленный код

Код в `services/db.ts` уже обновлен! Все методы теперь используют новые SQL функции.

### Шаг 3: Протестировать

Протестируйте следующие функции:
- ✅ Удаление регистрации на курс
- ✅ Изменение приоритета регистрации
- ✅ Просмотр деталей студентов курса (админ)
- ✅ Просмотр всех пользователей с деталями (админ)
- ✅ Просмотр очередей курсов
- ✅ Просмотр событий календаря

## 📊 Ожидаемые улучшения

- ⚡ **Снижение количества запросов на 50-70%**
- ⚡ **Улучшение времени ответа на 50-70%** для админских запросов
- ⚡ **Уменьшение нагрузки на БД на 40-60%**

## ⚠️ Важно

- ⚠️ **Сделайте backup базы данных** перед применением SQL функций
- ⚠️ Протестируйте на тестовой среде перед продакшеном
- ⚠️ Все SQL функции используют `SECURITY DEFINER` для обхода RLS

## 🔍 Проверка

После применения SQL функций, выполните этот запрос для проверки:

```sql
SELECT 
  routine_name,
  routine_type
FROM information_schema.routines
WHERE routine_schema = 'public'
  AND routine_name IN (
    'update_registration_priorities',
    'get_course_student_details',
    'get_all_users_with_details',
    'get_course_queue_counts',
    'get_calendar_events_with_creators'
  )
ORDER BY routine_name;
```

Должно вернуться 5 функций.

## 📁 Файлы

- **`optimization-functions.sql`** - SQL функции для применения в Supabase
- **`services/db.ts`** - уже обновлен с использованием новых функций

## ❓ Проблемы?

Если возникнут ошибки:
1. Проверьте, что все функции созданы успешно
2. Проверьте права доступа (GRANT EXECUTE)
3. Проверьте логи в консоли браузера
