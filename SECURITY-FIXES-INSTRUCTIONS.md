# Инструкция по исправлению предупреждений безопасности

## ✅ Что было исправлено

### 1. Function Search Path Mutable (5 функций)

Все функции теперь имеют `SET search_path = public`, что предотвращает атаки через манипуляцию search_path.

**Исправленные функции:**
- ✅ `update_updated_at_column()`
- ✅ `handle_new_user()`
- ✅ `update_course_translations_updated_at()`
- ✅ `get_course_queue_counts()`
- ✅ `setup_demo_user()`

**Файлы обновлены:**
- `supabase-complete-schema.sql` - основной файл схемы (для новых установок)
- `fix-security-warnings.sql` - миграция для существующих баз данных

## 🔧 Как применить исправления

### Для существующей базы данных:

1. Откройте **Supabase Dashboard** → **SQL Editor**
2. Скопируйте содержимое файла `fix-security-warnings.sql`
3. Вставьте в SQL Editor и нажмите **Run**
4. Проверьте Security Advisor - предупреждения должны исчезнуть

### Для новых установок:

Файл `supabase-complete-schema.sql` уже содержит исправления, просто используйте его как обычно.

## ⚠️ Требуется ручное исправление

### 2. Leaked Password Protection Disabled

Это предупреждение **нельзя исправить через SQL**. Нужно включить в Supabase Dashboard:

1. Откройте **Supabase Dashboard**
2. Перейдите в **Authentication** → **Settings**
3. Найдите раздел **Password Protection** или **Security**
4. Включите **"Leaked Password Protection"** (или "Have I Been Pwned" protection)
5. Сохраните изменения

**Почему это важно:**
- Защищает от credential stuffing атак
- Проверяет пароли пользователей на наличие в известных утечках данных
- Критически важно для production окружения

## 📊 Результат

После применения всех исправлений:
- ✅ 5 предупреждений "Function Search Path Mutable" будут исправлены
- ⚠️ 1 предупреждение "Leaked Password Protection Disabled" нужно включить вручную в Dashboard

## 🔒 Почему это важно

**Function Search Path Mutable:**
- Без фиксированного `search_path` злоумышленник может создать схему с вредоносными функциями
- При вызове функции PostgreSQL может использовать функции из схемы злоумышленника вместо ожидаемых
- Это серьезная уязвимость безопасности в production

**Leaked Password Protection:**
- Защищает от использования паролей, которые уже были скомпрометированы
- Предотвращает успешные атаки credential stuffing
- Рекомендуется OWASP и другими стандартами безопасности
